---
name: Local dev setup
description: Local environment details — Python version, Docker, venv location
type: reference
---

- Python 3.12.13 installed via Homebrew (`python3.12`)
- System Python is 3.9.6 (`/usr/bin/python3`) — do not use for this project
- Backend venv at `backend/.venv/` (created with `python3.12 -m venv`)
- Docker Desktop installed (as of 2026-05-28)
- GitHub auth via `gh auth login` (HTTPS with gh credential helper)
